A few quick questions if you don't mind:

  <%= form_for @user, :url => users_path, :html => { :multipart => true } do |form| %>
    <div class="field">
      <%= f.label :your_name %> <br>
      <%= f.text_field :name %>
    </div>

    <div class="field">
      <p><%= @event.question_1 %></p>
      <%= f.text_field :first_answer %>
    </div>

    <div class="field">
      <p> <%= @event.question_2 %></p>
      <%= f.text_field :second_answer %>
    </div>

    <div class="field">
      <p><%= @event.question_3 %></p>
      <%= f.text_field :third_answer %>
    </div>

    <div class="field">
      <%= f.label :take_a_picture_of_yourself %>
<!--       <input type="file" name="image" accept="image/*" capture="camera">
 -->
      <%= form.file_field :image %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
